
import AppRoutes from './AppRoutes';
import Layout from '../components/Layout';

const AppContent = () => {
  return (
    <Layout>
      <AppRoutes />
    </Layout>
  );
};

export default AppContent
